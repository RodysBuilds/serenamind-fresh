window.SerenaVault={async createKey(){return crypto.subtle.generateKey({name:'AES-GCM',length:256},true,['encrypt','decrypt'])},async encrypt(key,value){const iv=crypto.getRandomValues(new Uint8Array(12)),data=new TextEncoder().encode(JSON.stringify(value)),encrypted=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,data);return{ciphertext:btoa(String.fromCharCode(...new Uint8Array(encrypted))),nonce:btoa(String.fromCharCode(...iv)),algorithm:'AES-GCM'}},async decrypt(key,record){const iv=Uint8Array.from(atob(record.nonce),c=>c.charCodeAt(0)),bytes=Uint8Array.from(atob(record.ciphertext),c=>c.charCodeAt(0)),plain=await crypto.subtle.decrypt({name:'AES-GCM',iv},key,bytes);return JSON.parse(new TextDecoder().decode(plain))}};